from django.apps import AppConfig


class ProspectListsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.prospect_lists"
    verbose_name = "Prospect Lists"
